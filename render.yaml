services:
  - type: web
    name: trialatportfolio
    env: node
    rootDir: .
    buildCommand: npm run build
    startCommand: serve -s build
    envVars:
      - key: NODE_VERSION
        value: 18
