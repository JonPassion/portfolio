services:
  - type: web
    name: portfolio
    env: node
    rootDir: .
    installCommand: npm install
    buildCommand: npm run build
    startCommand: serve -s build
    envVars:
      - key: NODE_VERSION
        value: 18
